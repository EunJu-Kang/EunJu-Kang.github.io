---
layout: post
title:  "포켓몬"
subtitle:   ""
categories: Algorithm
tags: PROMS
---

## 프로그래머스 [포켓몬]
[문제보기](https://school.programmers.co.kr/learn/courses/30/lessons/1845)  
![Alt text](/../assets/img/programmers/포켓몬.png)

### 소스코드

~~~ java
import java.util.HashSet;
import java.util.Set;

class Solution {
     public int solution(int[] nums) {
        int limitCnt = nums.length / 2;

        // 중복 제거
        Set<Integer> numSet = new HashSet<>();

        for (int num : nums) {
            if (numSet.contains(num)) {
                continue;
            }

            numSet.add(num);
        }

        // 중복을 제거한 포켓몬 종류가 제한 갯수보다 많은지 확인
        if (numSet.size() <= limitCnt) {
            return numSet.size();
        } else {
            return limitCnt;
        }
    }
}
~~~
