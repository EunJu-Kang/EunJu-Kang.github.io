---
layout: post
title:  "타겟 넘버"
subtitle:   ""
categories: Algorithm
tags: PROMS
---

## 프로그래머스 [타겟 넘버]
[문제보기](https://school.programmers.co.kr/learn/courses/30/lessons/43165)  
![Alt text](/../assets/img/programmers/타겟_넘버.png)

### 소스코드

~~~ java
class Solution {
    private static int answer;

    public static int solution(int[] numbers, int target) {
        answer = 0;
        calcNumber(numbers, target, 0, 0);
        return answer;
    }

    private static void calcNumber(int[] numbers, int target, int total, int idx) {
        if (idx >= numbers.length) {
            if (total == target) {
                answer++;
            }
            return;
        }

        int num = numbers[idx];
        calcNumber(numbers, target, total + num, idx + 1);
        calcNumber(numbers, target, total - num, idx + 1);
    }
}
~~~
